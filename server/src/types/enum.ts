export enum Season {
  Spring,
  Summer,
  Winter,
  Fall,
}

export enum AnimeFormat {
  TV,
  Tv_short,
  Movie,
  Special,
  Ova,
  Ona,
}

export enum AnimeStatus {
  Finished,
  Releasing,
  To_be_released,
  Cancelled,
  Hiatus,
}

export enum UserType {
  SuperUser,
  Admin,
}
